<Page x:Class="RentacarApp.View.ReportsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:RentacarApp.View"
      mc:Ignorable="d" 
      d:DesignHeight="550" d:DesignWidth="750"
      Title="Отчеты">

    <Grid>
        <!--<DataGrid x:Name="MarksDataGrid" Grid.Row="1" AutoGenerateColumns="False" ColumnWidth="*" IsReadOnly="True"
               VerticalScrollBarVisibility="Auto" SelectionMode="Single" SelectionChanged="MarksDataGrid_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ФИО студента" Binding="{Binding Users.FIO}"/>
                <DataGridTextColumn Header="Оценка" Binding="{Binding MarkValue}"/>
            </DataGrid.Columns>
        </DataGrid>
        <Button Width="200px" Height="30px" Margin="600,420,0,0" Click="SaveClick">Сохранить в виде excel</Button>
        <StackPanel HorizontalAlignment="Left" VerticalAlignment="Bottom">
            <TextBlock Width="100px" Height="20px">Группа:</TextBlock>
            <ComboBox Name="GroupsComboBox" SelectionChanged="GroupsComboBoxSelectionChanged" Width="100px" Height="20px"></ComboBox>
        </StackPanel>-->
    </Grid>
</Page>
